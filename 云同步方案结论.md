# Plant Talk —— 云同步方案结论与展望

> 状态:**已定方向,暂缓实施**。本文档记录 2026-07 讨论得出的结论、做法与展望,供日后落地时直接参照。
> 决策人:项目作者。执行:待鸿蒙端推进后再回来实施。

---

## 0. 一句话结论

用**阿里云**搭一个轻量、常在线的"同步信箱":对话历史必上云、传感器历史按需上云、记忆走"云存共享 + 客户端增量整理"。目的是让 iOS / Web / 鸿蒙三端共享对话与记忆、在外也能看数据、记忆只整理一次省 token。**云不是数据的唯一家**,各端保留完整本地副本,云挂了也能离线用(local-first)。

---

## 1. 为什么要上云(需求回溯)

- **用户设备使用习惯**:电脑不常开;手机蓝牙不常连 ESP32。ESP32 常年待在家、插着电。
- **ESP32 连接特性**:标准 BLE peripheral,**同一时刻只能连一个** central(iOS 或 Web,二选一),连上后按 `sequence` 游标增量同步历史。
- **三端割裂现状**:
  - iOS 对话存本地 SQLite;Web 对话存本地 IndexedDB;**两端对话互不相通**。
  - **记忆功能只有 iOS 有**(整理器 + `plant-memory.json` + 注入 prompt);Web 完全没有。
  - 所以现状不是"两边重复整理浪费 token",而是"一边有、一边没有"。
- **三个目标**:
  1. 对话 + 记忆三端共享;
  2. 在外(不在家、不同局域网)也能看植物数据;
  3. 记忆只整理一次,省 LLM token。

## 2. 关键决策与理由

### 决策 A:走云,不走局域网 P2P
- 用户明确"在外看数据就够"。人在外面时手机和家里电脑不在同一局域网,**局域网 P2P 结构上满足不了"在外看"**。
- 要"在外能看",数据必须待在**常在线**的地方。用户不想给 ESP32 加 WiFi,那常在线处只能是云。
- 且需覆盖 Web 端,而浏览器沙箱做不了 P2P(不能监听端口 / mDNS 发现)。云是唯一自洽方案。

### 决策 B:local-first,云只当"同步信箱"
- 每端保留完整本地副本(iOS SQLite / Web IndexedDB / 鸿蒙 relationalStore),现状不变。
- 云只做常在线中转:各端 push 新增、pull 所需。云挂了/不想用了,各端离线照常工作。
- 消解"数据被云绑架"的顾虑。

### 决策 C:对话必上,传感器按需,记忆走"云存共享 + 客户端算"
- **对话历史:上云**(记忆共享 + 远程看的地基)。对话 append-only、每条有唯一 `id` + `createdAt`,按 id 去重、按时间排序即可,**无写冲突**,与 ESP32 的 `sequence` 去重同思路。
- **传感器历史:按需**。想在外看温湿度曲线 → 客户端把从 ESP32 同步下来的读数转存一份到云;只在家看 → 保持 ESP32 BLE 现状,不上云,少一摊事。**ESP32 那套 BLE 增量同步已成熟,不要动。**
- **记忆:不在云端函数里跑整理,改为"云端只存共享的 {记忆文档 + 游标},整理仍由当前在线的客户端按游标增量做"**。
  - 理由 ①:绕开云函数的执行时长/CPU 限制,免费额度稳够;
  - 理由 ②:不用把 iOS 那套 851 行对账逻辑移植成云端代码;
  - 理由 ③:谁先打开客户端谁算、别人发现游标已推进就跳过 → 同样"只算一次、省 token";
  - 理由 ④:记忆只在打开客户端时才被用到,"没人开客户端时也更新记忆"没有意义,故**定时任务(cron)在本场景是过度设计**。

### 决策 D:云端读明文(放弃端到端加密)
- 记忆要"云端整理"和"端到端加密"二者矛盾,只能选一。
- 但按决策 C,整理已放回客户端,云端其实**只存不算**,是否读明文对功能无影响 → 默认存明文(实现最简)。
- 隐私论据:对话内容现在已明文发给 LLM 厂商(DeepSeek / 阿里云百炼),再在自己的云上存一份,边际隐私损失远小于既成事实。传感器温湿度光照不敏感。
- 若日后想加强:可对"记忆文档"这一项做客户端加密后再存云(因为它不需要云端计算),对话则维持明文。留作展望。

## 3. ⚠️ 重要区分:两个"阿里云"别混

| | 用途 | 现状 |
|---|---|---|
| **阿里云百炼 DashScope** | App 里的**实时语音 LLM**(`wss://dashscope.aliyuncs.com/...`) | 已在用,是"推理服务" |
| **阿里云 FC + Tablestore**(本方案) | **同步信箱**:存对话/记忆、供三端 push/pull | 待建,是"存储 + 接口服务" |

两者都在阿里云账号下、可共用余额,但**是不同产品、不同用途**。本方案指的是后者。

## 4. 选型:阿里云具体用什么

选阿里云的原因:账号有余额。对应 Cloudflare(D1 + Workers)的等价物:

| 角色 | 阿里云产品 | 说明 |
|---|---|---|
| 无服务器接口(push/pull HTTP) | **函数计算 FC**(HTTP 触发器) | 按 CU 计费,新用户有免费试用额度 |
| 数据存储 | **表格存储 Tablestore**(NoSQL,宽表)或 **RDS/PolarDB**(SQL) | Tablestore 更轻、更契合按 key 存取;习惯 SQL 可用 RDS |
| (可选)对象存储 | **OSS** | 若对话含图片附件,图片存 OSS、库里只存引用 |

**计费要点(2026-07 查证,以官网实时为准)**:
- FC 已从"GB-秒/调用次数"迁移到统一 **CU(计算单元)** 模型。新用户免费试用额度:**每月 15 万 CU,连续 3 个周期**,需在"新客试用套餐"页主动领取。超出转按量付费(阶梯单价约 0.00009~0.00011 元/CU)。
- CU 换算(弹性实例):vCPU 1.0 CU/(vCPU·秒)、内存 0.15 CU/(GB·秒)、调用 75 CU/万次。小数据量个人自用极难跑满免费额度。
- FC **定时触发器 / HTTP 触发器本身不额外收费**,成本来自被触发函数的 CU。
- Tablestore 有 VCU 模式(预留)和 CU 模式(按量);**免费额度数值以定价页实时为准**(见文末链接),历史上按量模式有每月免费读写 + 存储额度。

> 提醒:免费额度和计费规则阿里云调整较频繁,**落地前务必去控制台 / 定价页核对当下数值**,不要照本文档的数字直接规划成本。

## 5. 落地做法(待实施时的步骤蓝图)

> 尚未动手。以下为实施顺序建议。

1. **建存储表**(Tablestore)
   - `conversations`:主键 `conversation_id`;属性 `title, kind, created_at, updated_at`。
   - `messages`:主键 `(conversation_id, message_id)` 或 `(conversation_id, created_at, message_id)`;属性 `role, content, attachments(引用), tool_invocations`。
   - `memory`:单行(或按 user/device 分行),存 `{记忆文档 JSON, last_processed_sequence, updated_at}`。
   - (可选)`sensor_readings`:主键 `(device_id, sequence)`;属性 温/湿/光/土壤/时间戳。
2. **建 FC 函数 + HTTP 触发器**,暴露最小接口:
   - `POST /messages`:上传新消息(按 id 幂等去重)。
   - `GET /messages?since=<cursor>`:拉某会话/全局游标之后的新消息。
   - `GET /memory` / `PUT /memory`:读/写共享记忆文档(写时带 `expected_sequence` 做 compare-and-swap,防两端同时整理覆盖)。
   - (可选)传感器读数同款 push/pull。
3. **鉴权**:极简即可(单用户)。预共享 token 走 Header;OSS/图片用签名 URL。**至少要有鉴权,别裸奔公网**。
4. **客户端接入**(各端):
   - 对话:本地新增后 push;打开/下拉时 pull 合并(按 id 去重、按时间排序)。
   - 记忆:整理前先 `GET /memory` 取 {记忆, 游标};从本地/云拉游标后的新对话;仅对增量调 LLM 整理;`PUT /memory` 带 CAS 写回。游标已被别端推进 → 跳过,零 token。
5. **降级**:任一端离线 / 云不可用 → 用本地副本继续,恢复后再同步。

## 6. 同步一致性要点(避免踩坑)

- **对话**:append-only + 唯一 id → 天然无冲突,后到的按 id 去重即可。
- **记忆**:单写入者语义靠"游标 CAS"保证——写回时若云端游标已变,说明别端刚整理过,放弃本次(或重拉增量再算)。单用户很少撞上,退化为"后写覆盖"也可接受。
- **游标**:只增不退(照搬 ESP32 `sequence` 思路)。
- **删除**:本地删对话不应让记忆游标回退(iOS 用独立自增序列表 `ai_memory_message_sequences` 保证,云端同理用独立单调游标)。

## 7. 展望(未来可选增强)

- **远程实时控制 ESP32**:当前只做"在外看数据",不给 ESP32 加 WiFi。若日后要"在外立即采样/实时读数",需 ESP32 加 WiFi + 反向连 MQTT/WebSocket broker,固件改动较大。**非当前范围**。
- **记忆文档端到端加密**:记忆不需云端计算,可客户端加密后再存,兼顾隐私。对话维持明文。
- **Web 端补齐记忆功能**:接入共享记忆后,Web 也能读记忆、注入 prompt,甚至参与整理。
- **多用户 / 多设备**:当前按单用户设计;若扩展,存储表主键加 `user_id` 维度、鉴权升级。

## 8. 参考链接(计费,以实时为准)

- 函数计算 FC 新用户免费试用额度:https://help.aliyun.com/zh/functioncompute/fc/product-overview/trial-quota-1
- 函数计算 FC 计费说明:https://help.aliyun.com/zh/functioncompute/fc/product-overview/billing-overview-of-fc
- 表格存储 Tablestore 计费:https://help.aliyun.com/zh/tablestore/product-overview/billing-overview/
- 表格存储定价详情:https://www.aliyun.com/price/detail/ots
- FC 计费项/试用额度变更说明:https://help.aliyun.com/zh/functioncompute/fc/product-overview/product-changes-changes-of-billable-items-resource-plans-and-trial-quota-of-function-compute
