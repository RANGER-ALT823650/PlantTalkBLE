# Plant Talk 项目进度

更新日期：2026-07-10

## 一、项目目标

开发一套无人值守的植物感知系统：ESP32 定期采集植物及环境数据，通过 BLE 与 iPhone 通信；Swift App 查看实时和历史数据，并在后续结合植物照片、拍摄位姿和传感器数据交给大模型分析或进行实时对话。

当前阶段已经完成“传感器采集 → BLE 传输 → iPhone 实时显示 → 大模型流式分析与对话”的最小闭环。

## 二、硬件端（ESP32）

### 2.1 当前硬件

- ESP32-WROOM-32E 开发板
- SHT31 温湿度传感器
- 红板电阻式土壤湿度传感器
- BH1750 光照传感器（已焊接并在固件中启用，当前等待排查 I2C 连接）
- 面包板及杜邦线

### 2.2 当前接线

| 设备 | 传感器引脚 | ESP32 引脚 |
|---|---|---|
| SHT31 | VCC / GND | 3V3 / GND |
| SHT31 | SDA / SCL | GPIO21 / GPIO22 |
| 土壤湿度 | + / - | 3V3 / GND |
| 土壤湿度 | S | GPIO34（ADC） |
| BH1750（计划） | SDA / SCL | GPIO21 / GPIO22，共用 I2C |

所有传感器统一使用 3.3 V 供电，避免 GPIO 输入超过 3.3 V。

### 2.3 已完成

- [x] 串口初始化，波特率为 115200。
- [x] I2C 初始化：SDA = GPIO21，SCL = GPIO22。
- [x] I2C 地址扫描功能。
- [x] SHT31 温度和空气湿度采集，实际读数正常。
- [x] GPIO34 土壤湿度 ADC 原始值采集，实际读数正常。
- [x] 传感器初始化失败后继续运行并自动重试。
- [x] BLE 外设广播，设备名为 `Plant Sensor`。
- [x] 将正式采样周期设为 5 分钟，每次采样同时写入历史并更新 BLE 实时值。
- [x] 使用固定 16 字节二进制数据包，包含版本、有效标志、土壤 ADC、温度、湿度和预留光照字段。
- [x] 完成固定 20 字节历史记录、序号、时间戳、CRC 和批次 ACK 协议设计。
- [x] 使用 LittleFS `/history.bin` 实现固定槽位环形存储，启动时通过 CRC 扫描恢复状态。
- [x] 使用 NVS 按 1024 条预留序号，LittleFS 重建后也不会复用旧序号。
- [x] 实现历史控制 `...6003` 和历史通知 `...6004` characteristic。
- [x] 实现每批最多 64 条、等待 App SQLite 提交后再继续的增量同步。
- [x] 实现 iPhone 连接校时以及重启后估算时间标记。
- [x] 启动后等待最多 15 秒校时，再保存首条记录；无手机时使用估算时间。
- [x] 新固件使用 ESP32 Arduino Core 3.3.8 编译通过。
- [x] 旧版实时 BLE 固件已完成真机运行验证。

当前主固件：[`firmware/PlantSensorBLE/PlantSensorBLE.ino`](firmware/PlantSensorBLE/PlantSensorBLE.ino)

### 2.4 暂未完成

- [ ] 排查 BH1750 的 I2C 连接并验证 lux 数据（`ENABLE_BH1750` 已改为 `true`）。
- [ ] 将新历史固件烧录到 ESP32，并与 iPhone 完成真机端到端同步验证。
- [ ] 优化 BLE 广播间隔和功耗，目标是 BLE 长期可发现。
- [ ] 测量整块开发板的实际平均电流。
- [ ] 确定电池、充电保护模块及低静态电流稳压方案。
- [ ] 控制电阻式土壤传感器仅在采样时供电，降低电极腐蚀。
- [ ] 决定时间方案：手机连接校时、Wi-Fi NTP 或 DS3231 RTC。

### 2.5 当前硬件风险

- 当前开发板包含 USB 转串口芯片、电源灯和稳压器，整板功耗可能明显高于 ESP32 模组本身。
- BLE 长期广播不会明显损伤 ESP32，但会缩短电池续航，需要实测后再确定电池容量。
- 开发板 USB-C 接口不等于具备锂电池充电功能，不能在未确认电路的情况下直接连接锂电池。
- 电阻式土壤传感器长期通电会加速腐蚀。

## 三、软件端（Swift / iOS）

### 3.1 当前技术栈

- SwiftUI
- CoreBluetooth
- Observation
- GRDB + SQLite
- iOS 17+
- XcodeGen 管理工程配置

### 3.2 已完成

- [x] 创建可编译的 SwiftUI iOS 工程。
- [x] 配置蓝牙权限说明 `NSBluetoothAlwaysUsageDescription`。
- [x] 扫描并识别名为 `Plant Sensor` 的 BLE 外设。
- [x] 兼容通过设备名或自定义 Service UUID 识别 ESP32。
- [x] 实现扫描、连接、断开和错误状态显示。
- [x] 发现自定义 BLE Service 和 Data Characteristic。
- [x] 订阅 Notify，并在连接后主动读取当前值。
- [x] 解码 ESP32 的 16 字节 little-endian 数据包。
- [x] 实时显示温度、空气湿度、土壤 ADC 和光照状态。
- [x] 增加 12 秒扫描超时提示。
- [x] Xcode 模拟器和真机目标编译通过。
- [x] 已在真机 iPhone 上成功连接 ESP32 并接收数据。
- [x] 使用 GRDB 和 SQLite 建立本地传感器历史库及 schema migration。
- [x] 使用 `(device_id, sequence)` 唯一键实现历史记录幂等写入。
- [x] 在同一事务中批量写入历史记录并推进持久化游标。
- [x] 定义 20 字节历史记录、CRC、分批结束、请求和 ACK 协议。
- [x] App 支持每批 64 条增量下载，写入成功后再请求下一批。
- [x] 每次收到 ESP32 的五分钟实时通知后自动触发增量历史同步。
- [x] 数据库读写使用 GRDB 异步接口，不阻塞 SwiftUI / CoreBluetooth 主线程。
- [x] 历史列表使用 GRDB ValueObservation，数据库提交后自动刷新。
- [x] 增加历史同步进度、完成状态、错误提示和手动重试。
- [x] 连接后自动发送 UTC 校时，并将估算时间标记保存到 SQLite。
- [x] 启用 `bluetooth-central` 后台模式，并在意外断线后自动重连。
- [x] 增加协议解码、CRC 和数据库重复导入单元测试。
- [x] 支持配置兼容 OpenAI Chat Completions 协议的 Base URL、模型名称和百炼 / OpenAI API Key。
- [x] API Key 使用 iOS Keychain 保存，模型和系统 Prompt 使用本机偏好设置保存。
- [x] 支持自定义系统 Prompt（植物人格），并在请求中注入当前实时传感器数据。
- [x] 支持 SSE 流式文本对话、停止生成和当前植物状态分析。
- [x] 使用 GRDB + SQLite 保存、查看和删除历史会话与消息。
- [x] 适配 `qwen3.5-omni-flash-realtime` WebSocket 实时语音协议。
- [x] 实现麦克风 16 kHz PCM 输入、模型 24 kHz PCM 输出和全双工播放。
- [x] 支持服务端 VAD、语音打断、用户转录和模型回复实时显示。
- [x] 保存实时语音对话转录，并提供主页和会话页独立入口。
- [x] 完成无滚动主页面重构：移除页面标题，保留竖向 3:4 植物插画区和下方圆形主交互按钮。
- [x] 主交互按钮点按启动实时语音对话，再次点按打开默认半屏、可上拉全屏的转录；会话持续运行且不启动相机。
- [x] 修复首次冷启动期间麦克风尚未采集、`session.created` 握手顺序不完整导致第一次说话丢失的问题；连接前音频会缓存并在会话就绪后补发。
- [x] 将历史记录入口移到左上角，并汇总 ESP32、文字对话和实时语音对话历史。
- [x] 将主页面弹出输入框设为文字对话唯一入口，并建立独立 `TextConversationView` 作为唯一文字聊天界面。
- [x] 完成 iOS 26 Liquid Glass 组件化界面，并为旧系统及深浅色模式提供自适应降级。
- [x] 聊天气泡按文字内容自适应宽度，主页面键盘发送键直接发送消息。
- [x] 模型首段可见文字到达前在页面顶部显示三点思考动画，不再渲染旋转的空白气泡。
- [x] 参考逐帧录屏重做首次及后续消息发送动画：文字从输入位置飞入气泡，气泡同步缩放、上移和淡入，列表平滑跟随。
- [x] 文字发送动画支持“减弱动态效果”，并完成 iPhone 17 / iOS 26.5 模拟器目标编译及 12 项单元测试验证。
- [x] 对照 Telegram iOS 提交 `6e370e06` 完成聊天实现审计，采用稳定消息关联、0.3 秒发送节奏和分层状态出现方式。
- [x] 将流式 assistant 独立为可观察消息行，移除每个 token 对完整消息数组的写入、过滤和重复滚动动画。

当前工程：[`ios/PlantTalkBLE.xcodeproj`](ios/PlantTalkBLE.xcodeproj)

主要代码：

- [`ios/PlantTalkBLE/PlantBluetoothManager.swift`](ios/PlantTalkBLE/PlantBluetoothManager.swift)：BLE 扫描、连接和数据接收。
- [`ios/PlantTalkBLE/PlantReading.swift`](ios/PlantTalkBLE/PlantReading.swift)：数据模型及二进制协议解析。
- [`ios/PlantTalkBLE/PlantDatabase.swift`](ios/PlantTalkBLE/PlantDatabase.swift)：GRDB 数据库、migration、批量写入和同步游标。
- [`ios/PlantTalkBLE/HistoryTransferProtocol.swift`](ios/PlantTalkBLE/HistoryTransferProtocol.swift)：历史同步二进制协议。
- [`ios/PlantTalkBLE/ContentView.swift`](ios/PlantTalkBLE/ContentView.swift)：主页面、工具栏、植物状态展开及首次文字对话过渡。
- [`ios/PlantTalkBLE/TextConversationView.swift`](ios/PlantTalkBLE/TextConversationView.swift)：文字聊天、流式回复、液态玻璃气泡、发送动画和思考状态。
- [`ios/PlantTalkBLE/HistoryOverviewView.swift`](ios/PlantTalkBLE/HistoryOverviewView.swift)：ESP32、文字对话和实时语音历史总入口。
- [`docs/telegram-ios-reference-audit.md`](docs/telegram-ios-reference-audit.md)：Telegram iOS 对照源码、采用/拒绝项和性能审计证据。
- [`ios/PlantTalkBLE/OpenAICompatibleClient.swift`](ios/PlantTalkBLE/OpenAICompatibleClient.swift)：兼容 Chat Completions 的 SSE 流式客户端。
- [`ios/PlantTalkBLE/AIConfiguration.swift`](ios/PlantTalkBLE/AIConfiguration.swift)：模型配置、系统 Prompt 和 Keychain API Key。
- [`ios/PlantTalkBLE/QwenRealtimeConversation.swift`](ios/PlantTalkBLE/QwenRealtimeConversation.swift)：Qwen Realtime WebSocket 会话与事件处理。
- [`ios/PlantTalkBLE/RealtimeAudioIO.swift`](ios/PlantTalkBLE/RealtimeAudioIO.swift)：麦克风采集、PCM 转换和实时播放。
- [`ios/PlantTalkBLE/RealtimeConversationSheet.swift`](ios/PlantTalkBLE/RealtimeConversationSheet.swift)：实时语音对话的半屏转录与结束聊天界面。

### 3.3 暂未完成

- [ ] 设计历史记录列表、趋势图和时间范围筛选。
- [ ] 与加入历史 characteristic 的 ESP32 固件完成真机端到端同步测试。
- [ ] 增加设备电量、最后在线时间和存储使用量显示。
- [ ] 增加自动重连及连接恢复策略。
- [ ] 使用 ARKit 拍摄植物照片并记录手机相对植物的位姿。
- [ ] 通过视觉定位底垫或参考图建立植物坐标系。
- [ ] 计算拍摄距离、方位角、东北/西南等方向和摄像头俯仰角。
- [ ] 将照片、位姿、温湿度、土壤湿度及光照整合为一次观察记录。
- [ ] 接入照片、位姿和传感器观察记录，进行视觉大模型综合分析。
- [ ] 使用真实百炼 API Key 在 iPhone 真机完成实时语音端到端联调。

## 四、当前 BLE 协议

Service UUID：

```text
7A1E0001-7C6D-4A8B-9E1F-2D3C4B5A6000
```

Data Characteristic UUID：

```text
7A1E0002-7C6D-4A8B-9E1F-2D3C4B5A6000
```

单个通知为 16 字节：

| 字节 | 内容 |
|---|---|
| 0 | 协议版本，当前为 1 |
| 1 | 有效标志：bit0 = SHT31，bit1 = BH1750 |
| 2–3 | 土壤 ADC，UInt16 little-endian |
| 4–7 | 温度，Float32 little-endian |
| 8–11 | 空气湿度，Float32 little-endian |
| 12–15 | 光照 lux，Float32 little-endian |

历史同步使用控制 characteristic `...6003` 和历史通知 characteristic `...6004`，
每批最多 64 条，SQLite 提交后才发送 ACK。ESP32 和 App 两端代码均已实现，完整字节定义见
[`HUMAN_ONLY/history-sync-protocol.md`](HUMAN_ONLY/history-sync-protocol.md)，当前等待烧录和真机联调。

## 五、建议的下一阶段

1. 焊接并验证 BH1750，完成全部传感器输入。
2. 烧录当前历史固件，使用真机验证断线、重传、CRC 错误和 Flash 覆盖旧记录等场景。
3. 根据串口打印出的 LittleFS 容量确认实际 partition scheme 是否满足离线时长。
4. 在 Swift App 中加入历史趋势页面。
5. 测量长期 BLE 广播时的实际电流，再确定电池及电源方案。
6. 完成稳定的数据记录后，再开发 ARKit 拍照位姿和大模型分析。

## 六、当前里程碑

| 里程碑 | 状态 |
|---|---|
| 传感器串口 Demo | 已完成 |
| ESP32 BLE 实时传输 | 已完成 |
| Swift App 真机连接与实时显示 | 已完成 |
| BH1750 光照采集 | 等待硬件焊接 |
| ESP32 历史存储 | 代码与编译完成，等待真机烧录 |
| App 历史数据同步与持久化 | 已完成，等待真机联调 |
| App 历史趋势展示 | 未开始 |
| Swift App 主页面与文字聊天 UI | 当前版本已完成，等待真机体验调优 |
| 电池与低功耗优化 | 方案讨论中 |
| ARKit 拍照位姿 | 方案讨论中 |
| 大模型分析与实时对话 | 传感器分析、流式文本和 Qwen 实时语音已完成；视觉待开发 |
