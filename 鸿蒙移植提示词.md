# Plant Talk —— 从 iOS (SwiftUI) 移植到鸿蒙 (ArkTS / ArkUI) 的实现提示词

> 这份文档本身就是"提示词":把它连同仓库一起交给能读代码的 AI / 开发者,在 DevEco Studio 里生成 HarmonyOS NEXT 原生代码。

---

## 0. 给执行者的话(先读这段)

- **权威源码**:iOS 端 `ios/PlantTalkBLE/`(约 28 个 Swift 文件,是功能最全的客户端)。UI、动画、交互、记忆功能一律以它为准。
- **第一优先级 = 动画与交互流畅度**。开发者在动画上投入极大,必须 **1:1 复刻**,参数不许"大概",全部照抄本文档给出的数字,细节回源码 `file:line` 核对。目标真机 60fps 无掉帧。
- **可走的捷径**:纯底层的 BLE 协议编解码、LLM 客户端逻辑,建议参考 Web 端的 TypeScript 实现(`web/lib/ble/`、`web/lib/ai/`)改写——TS → ArkTS 几乎无痛,比从 Swift 翻译省力。UI / App 架构 / 记忆功能仍以 iOS 为准。
- **分阶段交付**,每阶段真机验证流畅度后再进下一阶段(见第 11 节)。
- **不要**臆造 ArkUI 里不存在的 API;拿不准的动画(如 MeshGradient)按本文档给的退化方案实现。

---

## 1. 这个 App 是什么

"和植物对话"的陪伴型 App。一台 ESP32 传感器设备(温/湿/光/土壤)通过 **BLE** 广播数据,App 连上后增量同步历史。核心体验是三块:

1. **文字对话**:与"植物人格"聊天,OpenAI 兼容流式接口。
2. **实时语音对话**:阿里云百炼(Qwen)realtime WebSocket,带音频驱动的可视化 Orb。
3. **传感器历史 + 长期记忆**:图表查看历史;大模型在后台把对话提炼成长期记忆,回注到对话上下文。

---

## 2. 技术栈与架构约定(ArkTS)

| iOS (SwiftUI) | 鸿蒙 (ArkTS / ArkUI) |
|---|---|
| `@State` / `@Binding` | `@State` / `@Link`(双向)、`@Prop`(单向) |
| `@Observable` 类 / `@StateObject` | `@Observed` 类 + `@ObjectLink` |
| `@AppStorage` | `AppStorage` + `@ohos.data.preferences` |
| `NavigationStack` / `NavigationLink` | `Navigation` 组件 + `NavPathStack`(或 `router`) |
| `.sheet` / `.fullScreenCover` | `bindSheet` / `bindContentCover` |
| SQLite (GRDB) | `@ohos.data.relationalStore` |
| `UserDefaults` | `@ohos.data.preferences` |
| JSON 文件 (Application Support) | `@ohos.file.fs`(记忆文档存这里) |
| CoreBluetooth | `@ohos.bluetooth.ble` |
| `URLSession` 流式 | `@ohos.net.http` / `fetch`,SSE 流式解析 |
| WebSocket (URLSessionWebSocketTask) | `@ohos.net.webSocket` |
| 系统动态色 (`.systemBackground` 等) | 资源限定 `resources/base` + `resources/dark` 双套色 |

**建议目录结构**
```
entry/src/main/ets/
  pages/            # 各页面(对应 iOS 的各 View)
  components/       # 复用组件(气泡、Orb、传感器卡、输入框…)
  animation/        # 动画曲线常量、转场定义(集中管理)
  theme/            # 设计 token(色/字/圆角/材质)
  ble/              # BLE 协议编解码 + 连接管理(参考 web/lib/ble/ 改写)
  ai/               # LLM 文本客户端 + 语音 realtime(参考 web/lib/ai/)
  data/             # relationalStore 建表、DAO、数据模型
  memory/           # 记忆存储 + 注入(见第 9 节,整理器可选)
  audio/            # PCM 采集/播放 + 音频电平分析(驱动 Orb)
```

---

## 3. 屏幕与导航地图

入口 `PlantTalkBLEApp.swift:51` → `ContentView`。全局强调色由 `AppTheme`(`PlantTalkBLEApp.swift:4-49`,6 主题色)经 `.tint` 注入。

根视图 `ContentView.swift:153-278` 是一个**自定义横向三页容器**(不是 TabView):历史页(左)↔ 主页(中)↔ 文字对话页(右),手势横滑切换。这套转场是灵魂,见 §6-B2。

| 屏幕 (iOS 文件) | 作用 | 导航方式 |
|---|---|---|
| `ContentView` (HomeDashboard) | 主页:植物插画 + 传感器卡片 + 输入框 | 根 |
| `TextConversationView` | 文字聊天(动画最密集) | 主页右滑 / 发送进入,自定义转场 |
| `HistoryOverviewView` | 历史总览(内含 3-tab TabView) | 主页左滑 / 时钟按钮,自定义转场 |
| `RealtimeConversationSheet` | 实时语音半屏 | `.sheet`(detents medium/large) |
| `AppSettingsView` → `AISettingsView` | 设置 / 大模型设置 | 系统 push |
| `MemoryDetailView` → `MemoryRecordEditorView` | 记忆管理 / 编辑 | 系统 push |
| `SensorChartSheet` | 传感器趋势图 | `.sheet(item:)` |
| `DetailView` / `DayDetailView` | 传感器历史列表 / 单日明细 | TabView + push |
| `ConversationListView` / `RealtimeConversationHistoryView` | 对话历史列表 | TabView + push |
| `PlantArtworkView` / `PlantArtworkEditorView` | 植物插画显示 / 裁剪编辑 | `.sheet` + `.photosPicker` |
| `CameraCaptureView` | 相机拍照 / 相册 / 实时视频 | `.fullScreenCover` |

---

## 4. 设计系统(先建好,全局复用)

iOS 端**没有集中的设计 token 文件**,散落各处。鸿蒙这边**务必先建 `theme/` 统一管理**,再画界面。

**配色**
- 主强调色:6 套主题(blue/green/yellow/pink/orange/purple),对应 iOS `UIColor.system*`(`PlantTalkBLEApp.swift:25-34`)。放资源 + `AppStorage("appTheme")` 切换,全局 tint。
- 语义色:iOS 全用系统动态色(`.systemBackground`/`.secondarySystemBackground`/`.separator`/`.tertiarySystemFill` 等)自动适配深浅色。鸿蒙用 `resources/base`(浅)+ `resources/dark`(深)两套色资源实现同等效果。
- 传感器卡片色(`ContentView.swift:1865-1892`):温度=orange、湿度=blue、土壤=brown、光照=yellow。
- Orb 深绿参考:`Color(hue:0.34, saturation:0.88, brightness:0.62)`;各主题 hue:蓝 0.62 / 紫 0.77 / 黄 0.13(brightness 0.80)/ 粉 0.94。

**字体**(全系统字体,无自定义字体文件)
- 语义字号沿用系统 `.body/.headline/.subheadline/.caption/.caption2/.footnote/.title2/.title3`。
- 特殊:图表大读数 `system size 42, weight bold, design rounded`(`SensorChartSheet.swift:127`);传感器卡片值 `title3 semibold monospacedDigit`(`ContentView.swift:1941`)。
- **所有数值显示用等宽数字**(`.monospacedDigit()`)——读数、时间、统计。鸿蒙 `.fontFeature("tnum")` 或等宽字体。

**圆角**(常用值,鸿蒙一律用平滑圆角近似 `.continuous`)
24 输入框 / 20 图表容器 / 18 传感器卡·气泡 / 17 附件缩略图 / 14·13 统计行·工具卡 / 9·7 工具子项·JSON块 / 32 sheet 圆角 / 22 相机预览。
气泡为动态值:`min(18, max(10, height*0.28))`。

**材质 / 模糊**(iOS 大量用 material,鸿蒙用 `backgroundBlurStyle` 对应)
- `.ultraThinMaterial` → `.backgroundBlurStyle(BlurStyle.Thin)` + 半透明底色(最常用)
- `.thinMaterial` → `BlurStyle.Regular`
- `.regularMaterial` → `BlurStyle.Regular/Thick`
- iOS 26 的 `.glassEffect(...)`(液态玻璃):**鸿蒙无对应,参考 iOS 每处的 `#available(iOS 26)` fallback 分支**(fallback 用 `.ultraThinMaterial` + 描边),照 fallback 实现即可。

**阴影 / 描边**
- 描边普遍:分隔色 opacity 0.25~0.5,lineWidth 0.5~1.5;气泡描边 lineWidth 0.5。
- 阴影按各处给的 radius/y 值(见 §6)。

**减弱动效(reduceMotion)**
iOS 广泛用 `@Environment(\.accessibilityReduceMotion)` 做动画降级(几乎每个动画都有降级分支)。鸿蒙侧:读取系统无障碍/动效设置(若平台未直接暴露"减弱动效"标志,则提供 App 内开关),为真时按各处给的"reduceMotion 分支"参数走简化动画。

---

## 5. ⭐ SwiftUI → ArkUI 动画原语映射速查表(最重要)

复刻手感的关键。**SwiftUI 的 spring 在鸿蒙里几乎 1:1**,照着填数即可。

| SwiftUI 写法 | 参数含义 | ArkUI 对应 | 备注 |
|---|---|---|---|
| `.spring(response:r, dampingFraction:f)` | 响应时长 / 阻尼比 | `curves.springMotion(r, f)` | **近乎 1:1**,直接填同样的数 |
| `.interactiveSpring(response:r, dampingFraction:f)` | 手势跟随弹簧 | `curves.responsiveSpringMotion(r, f)`(或 `springMotion`) | 手势结束时用 `PanGesture` 的 `velocityX/Y` 做甩动落定 |
| `.smooth(duration:d)` | = spring bounce 0(无回弹) | `curves.springMotion(d, 1.0)`,或 `Curve.EaseInOut` + duration d | 平滑无弹跳 |
| `.snappy(duration:d, extraBounce:e)` | 轻微回弹 | `curves.springMotion(d, ≈0.82)` | e 越大阻尼越小(回弹越多) |
| `.easeInOut(duration:d)` | | `animateTo({duration:d*1000, curve:Curve.EaseInOut})` | duration 单位换成毫秒 |
| `.easeOut(duration:d)` | | `Curve.EaseOut` | |
| `.linear(duration:d)` | | `Curve.Linear` | |
| `.timingCurve(a,b,c,d, duration:t)` | 三次贝塞尔 | `curves.cubicBezierCurve(a,b,c,d)` + duration `t*1000` | 控制点直接照抄 |
| `withAnimation(a){ …改状态… }` | | `this.getUIContext().animateTo({…curve…}, () => { …改状态… })` | |
| `.animation(a, value:v)` | 值变化触发 | `@Watch("v")` 里调 `animateTo` | |
| `.repeatForever(autoreverses:true)` | 无限往返 | `animateTo({iterations:-1, playMode:PlayMode.Alternate, …})` | |
| `transition(.move(edge:.x))` | | `TransitionEffect.move(TransitionEdge.X)` | |
| `transition(.scale(s, anchor:))` | | `TransitionEffect.scale({x:s, y:s})` + `transformCenter` | |
| `transition(.opacity)` | | `TransitionEffect.opacity(0)` | |
| 组合 transition(`+`) | | `TransitionEffect.a.combine(b)` | |
| `matchedGeometryEffect` / `.glassEffectID` | 共享元素形变 | `geometryTransition('id')` | **1:1 真有此特性!** 用于媒体形变、玻璃按钮过渡 |
| `TimelineView(.animation) + Canvas` | 逐帧手绘 | `Canvas` + 帧回调 / `@ohos.animator` 驱动重绘 | 见 §6-B1 |
| `MeshGradient(4×4)` | 网格渐变 | **ArkUI 无原生** → 用 iOS 已有的 `RadialGradient` fallback 路径作基准(`ContentView.swift:2119-2137`) | 别硬啃 mesh |
| `.contentTransition(.numericText())` | 数字滚动 | 自定义数字滚动组件,或退化为淡入 | 非核心 |
| `.symbolEffect(.variableColor)` | SF Symbol 动效 | 自绘动画图标 | 无 SF Symbols |

**手势映射**

| SwiftUI | ArkUI | 备注 |
|---|---|---|
| `DragGesture(minimumDistance:d)` | `PanGesture({distance:d})` | `onActionUpdate` 拿 offset,`onActionEnd` 拿 `velocityX/Y` |
| `MagnifyGesture` | `PinchGesture` | 缩放 |
| `simultaneousGesture` | `GestureGroup(GestureMode.Parallel, …)` | 并行识别 |
| 长按 `contextMenu` | `LongPressGesture` + `bindContextMenu` | |
| `.onTapGesture` | `TapGesture` | |
| `predictedEndTranslation`(甩动预测) | 用 `onActionEnd` 的 velocity 估算落点 | 快速滑动切页判定 |

---

## 6. 动画清单(逐个复刻,参数照抄)

按移植优先级排。每条给了 `file:line`,细节(如贝塞尔控制点、mesh 各点系数)回源码核对。

### 🔴 P0 — 灵魂级,必须精确

#### B1. 语音可视化 Orb(音频振幅联动,最高价值)

主体 `DiffuseOrb`(`ContentView.swift:2050-2201`),30 FPS 逐帧手绘:

- **驱动**:`TimelineView(.animation minimumInterval: 1/30, paused: reduceMotion||transitioning)`。鸿蒙用 `Canvas` + 帧回调(约 33ms 一帧),转场时暂停省电。
- **相位与呼吸**(`:2065-2071`):
  - `time = 当前时间秒`(reduceMotion 时 = 0)
  - `phase = time * (0.34 + 0.28 * activity)`(activity 越高转越快)
  - `breathing = clamp(visualLevel,0,1) * 0.012` ← **音频电平驱动的呼吸缩放**
  - `pulse`(仅 strongPulse 强调):`accentEnvelope * (0.035 + 0.02 * intensity)`
  - 最终:`scale = 1.06 * (1 + breathing + pulse)`(`:2082,2088`)
- **activity 值**(由连接态定,`:2034-2043`):已连=1、准备中=0.62、空闲/错误=0.34。
- **tint**(`:2023-2032`):已连=绿、准备中=橙、空闲/错误=强调色。
- **渐变**:iOS 18+ 用 4×4 `MeshGradient`(16 顶点 + 16 softening 混白值 `[0.12,0.18,0.3,0.22,0.25,0.38,0.56,0.32,0.78,0.62,0.88,0.76,0.9,0.94,1,0.92]`,`:2098-2112`);顶点用 `sin/cos(phase*系数)` 抖动(`meshPoints` `:2139-2182`,各点系数如 `sin(phase*0.73)`、`cos(phase*0.91)`,**逐个照抄**,边缘点 drift×0.4~0.45、内部点×1.0)。
  - **鸿蒙实现**:MeshGradient 无原生对应,直接用 iOS 的 **RadialGradient fallback**(`:2119-2137`,endRadius 160)作基准,在 Canvas 里画分层径向渐变 + 呼吸缩放,足够还原"发光呼吸球"观感。若行有余力再用多层叠加逼近 mesh。
- **描边**:`white opacity 0.28, lineWidth 1`(`:2085`);**外阴影**:`tint opacity 0.25, radius width*0.12, y width*0.05`(`:2000-2004`)。
- **accent 包络**(`:2184-2196`):attack 阶段 strongPulse 0.09s / colorShift 0.07s(smoothstep `p*p*(3-2p)`);release strongPulse 0.5s / colorShift 0.34s(`(1-p)²` 衰减)。

音频→视觉驱动器 `VoiceVisualDriver`(`RealtimeAudioIO.swift:59-195`),鸿蒙在 `audio/` 里复刻:
- 电平(驱动呼吸):`target = clamp((dBFS+55)/43, 0,1)`,平滑 `level = 旧*0.78 + target*0.22`(`:158-161`)。dBFS 由 PCM 帧 RMS 算:`20*log10(rms)`,静音 -160(`AudioLevelAnalyzer` `:24-54`)。
- 双包络检突起:`fast = 旧*0.55 + level*0.45`、`slow = 旧*0.93 + level*0.07`、`onset = fast - slow`(`:82-146`)。
- accent 稀疏事件:strongPulse 阈值 assistant 4.2 / user 5.5,冷却 0.65s,4s 内≤3 次;colorShift 阈值 2.0 / 2.8,冷却 0.35s。落点从网格内部点 `[5,6,9,10]` 随机(`:163-194`)。

其它语音动效:
- 状态圆点脉冲 `RealtimeVoiceStatusSymbol`(`RealtimeConversationSheet.swift:609-631`):说话时 `scale 1→1.18`,`easeInOut(0.55).repeatForever(autoreverses)`。

#### B2. 自定义横向三页转场(核心交互,最复杂)

`ContentView.swift`:
- **手势**:`PanGesture(distance:12)`(`:423,433`)。水平判定:`|dx| > |dy| * 1.25`(`:850-852`)。
- **完成阈值**(`:558-568`):拖动 > `pageWidth*0.33`,**或**(拖动 > 12 且预测末位移 > `pageWidth*0.5`)。
- **回弹落定**(手势驱动,`:1029-1033`):`.interactiveSpring(response:0.32, dampingFraction:0.9)` → 鸿蒙 `curves.responsiveSpringMotion(0.32, 0.9)`;reduceMotion → `easeOut(0.16)`。
- **按钮触发转场**(非手势,`:1025-1027`):`.smooth(duration:0.35)`;reduceMotion → `easeInOut(0.2)`。
- **偏移驱动**(`InteractivePageOffsetModifier` `:1263-1317`):历史页基准 `-pageWidth`,文字页基准 `+pageWidth`,拖动位移夹取到中间(硬夹,无橡皮筋)。
- **历史页进场**:`.move(edge:.leading)`(reduceMotion `.opacity`)。
- **性能**:转场进行中暂停 Orb 的帧循环、冻结布局高度(`:418-420, 1592-1597`)——鸿蒙同样处理。

### 🟠 P1 — 高级感来源

#### B3. 消息飞行(输入框 → 聊天气泡"飞入")

- **等几何稳定**(`:1099-1156`):循环最多 30 次、每次等 16ms,目标 frame 连续 2 帧稳定(容差 0.5pt)才启动。
- **飞行**(`:1158-1191`):`.smooth(duration:0.32)`,插值 `currentFrame`(位置+尺寸)与 `progress` 0→1。
- **视觉**(`MessageFlightOverlay` `:1760-1789`):padding leading `18-4*progress`、trailing `18+2*progress`;背景 `.ultraThinMaterial + accent.opacity(0.2*progress)`;描边 `accent.opacity(0.32*progress), lineWidth 0.5`;形状 = 尾巴在右的气泡。
- **两种发送**(`:1680-1683`):在底部 → 飞行;不在底部 → 直接滚到底(`.scrollToBottom`)。由"是否滚到底"决定。

#### B4. 文字对话页内动画(TextConversationView)

- **输入框附件菜单**展开/收起:`.spring(response:0.42, dampingFraction:0.82)`(`:1299-1303`),菜单按钮 `.move(edge:.leading)` 进场;展开时输入框 `offset x -1000` 移出屏。玻璃按钮用 `geometryTransition`(对应 `.glassEffectID + .matchedGeometry`)。
- **媒体面板形变**(相机/相册按钮长大成大面板,`ConversationMediaMorphModifier` `:36-142`):`.linear(duration:0.4)`,分阶段 phase 插值(smoothstep),各属性区间(`:52-58`):宽 0→0.78、高 0.06→1、位置 0→0.82、内容淡入 0.18→0.56、表面色 0.06→0.34、图标淡出 0→0.18、阴影 0.12→0.72、圆角 0.14→1;阴影 `black.opacity(0.22*p), radius 24*p, y 12*p`。**用 `geometryTransition` 实现共享元素形变最省力**。
- **附件转移**(面板 → 输入框附件条,`:424-542`):`.timingCurve(0.22,0.72,0.22,1, duration:0.48)` → `curves.cubicBezierCurve(0.22,0.72,0.22,1)`;phase 区间见 `:440-447`。
- **图片预览**(缩略图 → 全屏,`:274-331`):`.smooth(duration:0.42)`(reduceMotion `easeInOut 0.18`),圆角从 17 插到屏幕圆角。
- **"模型思考中"三点跳动**(`ModelThinkingIndicator` `:3833-3877`):每 **180ms** 切激活点,`easeInOut(0.18)`;激活点 `scale 1 / offset y -2 / opacity 1`,非激活 `0.65 / +1 / 0.5`;进场 `.move(top)+.scale(0.9,anchor:top)+.opacity`。
- **流式打字节奏**(`StreamingAssistantState` `:2372-2459`):首 token 立即显示,后续 delta 合并、每 **80ms** flush 一次(`:2440`);文字增长本身**不加动画**(`transaction animation=nil`)。
- **自动滚到底**(`:2173-2181`):普通 `easeOut(0.2)`;catch-up `easeOut(0.24)`;飞行/初始时 `nil`。是否在底部检测容差 32pt,决定发送走飞行还是直滚。
- **工具调用展开**:`easeInOut(0.18)`,`.opacity+.move(top)`(`:3537-3648`)。

#### B5. 主页 Dashboard 展开/收起

`HomeDashboard`(`:1483-1625`):`.smooth(duration:0.42)`;插画宽度 `contentWidth*(展开?0.38:0.66)`;输入框进场 `.move(top)+.scale(0.94,anchor:top)+.opacity`;传感器网格进场 `.move(bottom)+.scale(0.94,anchor:top)+.opacity`。

### 🟡 P2 — 细节打磨

- **相机面板**(`CameraCaptureView.swift`):选项菜单 `.snappy(duration:0.24, extraBounce:0.08)`;选项按钮 `.move(bottom)+.scale(0.8,anchor:bottom)+.opacity`;快门 外圈 76×76 白 opacity 0.2 + 内圈 62×62 白(**无按压缩放**);相册选中角标 `.scale+.opacity`,切换 `easeOut(0.16)`。
- **实时语音 sheet**:detents `[medium, large]`、圆角 32、显示拖拽指示条;摄像头区展开 `.snappy(0.28)`,`.move(top)+.opacity`。
- **图表**(`SensorChartSheet.swift`):加载 `easeInOut(0.3)`;大读数 `.contentTransition(.numericText())`(鸿蒙自定义数字滚动或淡入);折线插值 7 天=linear,其余=catmullRom(平滑曲线)。
- **传感器卡片按压**(`SensorCardButtonStyle` `:1970-1977`):`scale 0.96 + opacity 0.85`,`easeInOut(0.15)`;描边按压时 `opacity 0.5 / lineWidth 1.5`,常态 `0.15 / 1`。
- **插画编辑**:双指缩放 `PinchGesture`(范围 1~6),拖动 + 缩放 `GestureGroup(Parallel)`;offset 按图/框比例夹取(回弹吸附 `:63-78`);reset `.smooth(0.25)`。

> 参考曲线:`AnimationDemoView.swift:67` 有一条 `.timingCurve(0.23,1.0,0.32,1.0, duration:0.3)`(`scale 0.9→1 anchor bottomTrailing + offset y 14→0 + opacity 0.72→1`),可作气泡出现动画基准。

---

## 7. 自定义绘制(Canvas / Path)

- **聊天气泡 `ChatBubbleShape`**(`TextConversationView.swift:3944-4064`):`tailWidth=min(10, w*0.08)`、`tailHeight=min(12, h*0.3)`、`cornerRadius=min(18, max(10, h*0.28))`;尾巴在底部一侧(user 右 / assistant 左),两段 `addQuadCurve`。控制点系数(如 `body.maxX + (bounds.maxX-body.maxX)*0.2`)**回源码照抄**(`:3993-4063`)。鸿蒙用 `Path` / `Canvas` 绘制。
- **对话背景 `ChatConversationBackdrop`**(`:4066-4084`):系统背景 + 右上 `accent.opacity(0.16)` endRadius 420 径向 + 左下 `green.opacity(0.1)` endRadius 380 径向。
- **媒体形变形状 / 插画框形状**:圆角矩形,连续圆角,宽高圆角可独立动画。

---

## 8. 数据与后端接入(让 App 真能跑)

### 8.1 BLE(建议参考 Web TS 改写)

设备名 `"Plant Sensor"`。GATT:
- Service `7A1E0001-...`
- Live/Data(Read+Notify)`...0002`
- History Control(Write)`...0003`
- History Data(Notify)`...0004`

协议命令(写 Control):`0x10` 请求游标后记录(游标 u32 + limit u16)、`0x11` ACK 已持久化序号(u32)、`0x12` 校时(Unix 秒 u32)、`0x13` 立即采样。

历史记录 **20 字节定长**:`[0]版本 [1]类型0x01 [2]标志(bit0 SHT31/bit1 BH1750/bit2 时间估算) [3]CRC-8(多项式0x07,跳过byte3) [4-7]序号u32LE [8-11]时间戳u32LE [12-13]土壤ADC u16 [14-15]温度×100 i16 [16-17]湿度×100 u16 [18-19]光照lux u16`。实时包 16 字节(不落盘,float LE)。

同步:按 `sequence` 游标增量,每批≤64 条,**落库成功才发 0x11 ACK 再拉下一批**;`(device_id, sequence)` 唯一去重、游标只增不退。批次结束包 `0x02` 带首/末序号、条数、剩余、设备最新序号供校验。

> **捷径**:编解码 + 同步状态机直接照 `web/lib/ble/protocol.ts` 和 `web-bluetooth.ts`(TS)改写成 ArkTS,把 Web Bluetooth API 换成 `@ohos.bluetooth.ble`。权威协议说明见 `HUMAN_ONLY/history-sync-protocol.md`。

### 8.2 LLM

- **文本**:OpenAI 兼容 Chat Completions,流式(SSE)。配置项:Base URL、model(默认 `gpt-4.1-mini`)、API Key。参考 iOS `OpenAICompatibleClient.swift` / `AIConfiguration.swift`,或 `web/lib/ai/chat-client.ts`。
- **语音**:阿里云百炼(Qwen)realtime,WebSocket(默认 `wss://dashscope.aliyuncs.com/...`)。参考 iOS `QwenRealtimeConversation.swift` 或 `web/lib/ai/realtime-qwen.ts`。音频采集 16kHz PCM16、播放 24kHz。
- **工具调用**:参考 `web/lib/ai/tool-catalog.ts` / `tool-executor.ts`。

### 8.3 数据模型与存储(relationalStore 建表)

- 对话 `AIConversation`:`id, title, kind("text"|"realtime"), createdAt, updatedAt`(`ChatModels.swift:14-20`)。
- 消息 `ChatMessage`:`id, conversationID, role, content, createdAt, imageAttachments[], toolInvocations[]`(`:42-50`)。
- 传感器读数:见 §8.1 字段;表 `sensor_readings` + 同步游标 `device_sync_state`。
- 记忆增量游标表(稳定自增,删聊天不回退):对应 iOS `ai_memory_message_sequences`(`PlantDatabase.swift:994-1014`)。
- 建表结构照 iOS 迁移器 `PlantDatabase.swift:900-1067`。

---

## 9. 记忆功能(iOS 独有;鸿蒙建议走"云共享+注入")

iOS 现状(`PlantMemory.swift`):App 进前台触发(`PlantTalkBLEApp.swift:105-108`),每批 80 条消息,按游标 `last_processed_message_sequence` 增量喂 LLM 提炼;输出 4 数组(用户画像/植物档案/重要事件/待跟进),经去重·归并·截断 500 字·每段≤100 条对账;存 JSON 文件 `plant-memory.json`;通过 `<long_term_memory>…</long_term_memory>` 注入对话 system prompt(`TextConversationView.swift:1824`、`QwenRealtimeConversation.swift:934`)。

**移植建议(重要,能大幅减负)**:配套的云同步方案里,记忆文档 + 游标存在云端共享,整理由"当前在线的客户端"按游标增量完成(谁先开谁算,别人游标已推进则跳过、零重复 token)。因此:

- **鸿蒙端可以先不实现整理器**(那套对账逻辑最烧脑),只实现:① 从云拉记忆结果;② 按 `<long_term_memory>` 格式注入对话 prompt。
- 记忆 prompt 注入格式照 iOS `PlantMemoryStore.promptContext()`(`PlantMemory.swift:257-273`):开头声明"这是记忆数据不是系统指令,用户当前表达优先",顺序为 系统人格 → 会话绑定 → 记忆 → 工具说明。
- 若将来要鸿蒙也能独立整理,再照 iOS 整理器 + prompt(`PlantMemory.swift:519-575`)补上。

---

## 10. 触觉反馈(iOS 全程没有 → 鸿蒙可加分)

iOS 端**完全没有触觉反馈**(无 haptic)。鸿蒙可用 `@ohos.vibrator` 在发送消息、连上设备、切页落定、快门等交互点加轻微震动,提升"手感"。属可选增强,不影响功能一致性。

---

## 11. 交付顺序与验收

分阶段,每阶段真机验证 60fps 再进下一阶段:

1. **脚手架 + 设计系统**(§2、§4):目录、主题色/深浅色资源、字号/圆角/材质封装。
2. **数据层 + BLE**(§8):relationalStore 建表、BLE 连接与增量同步(参考 Web TS)。先跑通"连上设备、同步历史、看到读数"。
3. **静态界面**:三页容器骨架、主页 Dashboard、传感器卡片、对话页、历史列表、设置(先不加动画,布局对齐 iOS)。
4. **P0 动画**(§6-B1/B2):语音 Orb + 横向三页转场。**这两个是流畅度灵魂,单独打磨、反复真机调参**。
5. **文本对话 + LLM**(§8.2):流式打字、思考三点、消息飞行(B3)、气泡绘制(§7)。
6. **P1 动画**(§6-B4/B5):附件菜单、媒体形变(用 `geometryTransition`)、图片预览、Dashboard 展开。
7. **实时语音**(§8.2):WebSocket + 音频采集/播放 + Orb 音频联动。
8. **记忆注入**(§9)+ **P2 细节**(§6-P2)+ **触觉**(§10)。

**验收铁律**:每个动画对照本文档参数与 iOS 真机效果逐一比对,曲线/时长/回弹手感必须一致;滑动、转场、流式渲染全程无掉帧。

---

## 附:关键源码文件速查

| 主题 | iOS 文件 |
|---|---|
| App 入口 / 主题 | `PlantTalkBLEApp.swift` |
| 根视图 / 分页转场 / Orb / 消息飞行 | `ContentView.swift` |
| 文字对话(动画最密集) | `TextConversationView.swift` |
| 实时语音 sheet | `RealtimeConversationSheet.swift` |
| 音频 IO + 视觉驱动器 | `RealtimeAudioIO.swift` |
| 语音 realtime 客户端 | `QwenRealtimeConversation.swift` |
| 文本 LLM 客户端 / 配置 | `OpenAICompatibleClient.swift` / `AIConfiguration.swift` |
| BLE 连接 + 同步 | `PlantBluetoothManager.swift` |
| 数据库(建表/同步/记忆游标) | `PlantDatabase.swift` |
| 数据模型 | `ChatModels.swift` |
| 记忆(模型/存储/整理/注入/UI) | `PlantMemory.swift` |
| 图表 | `SensorChartSheet.swift` |
| 插画显示/编辑 | `PlantArtworkView.swift` |
| 相机/相册 | `CameraCaptureView.swift` |
| BLE 协议权威说明 | `HUMAN_ONLY/history-sync-protocol.md` |
| Web BLE(改写参考) | `web/lib/ble/protocol.ts`、`web/lib/ble/web-bluetooth.ts` |
| Web LLM(改写参考) | `web/lib/ai/` |
