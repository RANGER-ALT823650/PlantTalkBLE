# Software Build Preference

更新日期：2026-07-18

## 第一部分：软件构建偏好

1. **优先使用 SwiftUI 与可复用组件**  
   界面应由职责单一的 SwiftUI 组件组成。文字大小、控件尺寸和位置应优先使用系统字体、比例、弹性布局、安全区和容器约束，非必要不写死。

2. **保持简洁、聚焦的主页面**  
   主页面不显示 `Plant Talk` 标题且不可上下滚动。植物画面位于中上部，保持竖向 3:4 比例；主要圆形交互按钮位于中下部，点按启动实时语音，再次点按打开半屏文字记录，不启动相机。

3. **入口必须明确且唯一**  
   主页面弹出的文字输入框是文字对话的唯一入口，专用文字聊天页面是文字对话的唯一交互页面。主页面右上角不再保留重复的文字或实时语音入口。

4. **工具栏结构遵循指定位置**  
   历史记录入口放在左上角，并集中提供 ESP32 数据、文字对话和实时语音对话历史；蓝牙和设置等操作保留在右上角。

5. **采用原生、克制且相互独立的液态玻璃视觉**  
   iOS 26 及以上优先使用 SwiftUI 原生 Liquid Glass。玻璃材质主要用于输入框、按钮、气泡和状态组件，并为旧系统保留材质降级。默认让各玻璃控件独立渲染；只有明确需要多个形状融合或 morph 时，才在最小局部范围使用 `GlassEffectContainer`，禁止用一个容器包裹整个页面。所有组件都要适配深色与浅色模式。

6. **动画必须保持空间连续性**  
   页面切换和消息发送不能通过位置、尺寸或圆角不一致的临时覆盖层制造跳动。参考 iMessage：真实输入框留在原位或退出页面，正式气泡提前占位但不渲染玻璃，顶层只移动一个独立气泡快照；抵达后由同 UUID 的正式气泡原子接管。动画使用快速平滑减速，并支持“减弱动态效果”。

7. **聊天气泡由内容决定尺寸**  
   气泡宽度随文字长短自适应，短消息不应出现大面积空白；长消息在可用宽度内自然换行。时间等辅助信息不能使用会强制撑宽气泡的布局。

8. **键盘与流式状态应符合聊天习惯**  
   主页面键盘的发送键直接发送而不是换行；打开历史总览前先取消输入焦点并收起键盘。聊天页面发送后保持键盘和输入焦点，方便继续输入。实时语音进行时禁用主页面文字输入且不弹出键盘。模型输出首段可见文字前，在页面顶部显示三个跳动的小点，不显示旋转中的空气泡。

9. **以参考材料的实际动态为准**  
   当提供截图或录屏时，应抽取关键帧分析位置、尺寸、出现顺序和缓动节奏；颜色仅在明确要求时参考，不因参考图而破坏系统深浅色适配。

10. **实时语音面板保持轻量且可扩展**  
   实时语音记录默认以半屏 Sheet 打开，并允许上拉到全屏。状态、结束按钮和聊天气泡保持为相互独立的原生玻璃表面，不使用贯穿顶部或底部的厚重 `.bar`，空状态也不使用占据大面积的通用占位卡片。

11. **破坏性操作使用全局二次确认**  
   删除等重要且不可逆的操作必须使用居中的全局弹窗说明影响范围，并提供明确的取消与破坏性确认按钮，不使用底部操作菜单。

12. **交互以真机实际行为为准**  
    编译通过或 SwiftUI 声明存在不等于交互可用；对文字选择、长按、拖拽、菜单等系统手势，必须在真实容器与真机上验证。聊天文字选择使用不可编辑、可选择的 `UITextView`，点击选区外任意区域即撤销选中；若 SwiftUI 行为不稳定，优先使用对应原生视图承载交互。

13. **半屏 Sheet 的退出与视觉语言**
    半屏 Sheet 的退出按钮放在右上角。能用符号表意时不用文字（优先 SF Symbols）；整体设计始终保持简约、大气。

## 第二部分：对应实现

1. **SwiftUI 与响应式布局**  
   主页面和聊天页面使用 SwiftUI 组合视图、`GeometryReader`、弹性间距、比例约束、系统字体和安全区布局。植物画框使用 `aspectRatio(3 / 4)`，主要尺寸由可用宽高推导。

2. **主页面结构和交互**  
   `ContentView.swift` 中的 `HomeDashboard` 使用不可滚动的全屏布局；`PlantArtworkPlaceholder` 保留植物插画区域；`PrimaryInteractionButton` 以单次点按启动实时语音，不开启摄像头。会话状态由 `ContentView` 持有并独立于页面展示持续运行，再次点按按钮打开默认半屏、可上拉全屏的转录 Sheet，只有 Sheet 底部的结束按钮会停止会话。

3. **唯一文字对话流**  
   植物详情展开后显示 `HomeTextComposer`。发送首条消息后创建 `TextConversationView`，后续输入只在该页面底部完成；左下角 Home 按钮返回主页面。

4. **工具栏与历史记录**  
   历史入口使用左上角 `ToolbarItem` 打开 `HistoryOverviewView`，由该页面汇总 ESP32、文字对话和实时语音历史；右上角只保留蓝牙与设置操作。

5. **Liquid Glass 与外观适配**  
   iOS 26 的输入框使用 `.glassEffect(.regular.interactive())`，底部 Home 与发送按钮使用 `.buttonStyle(.glass)`；旧系统使用 `ultraThinMaterial`、`.bordered` 与系统动态颜色。全屏 `GlassEffectContainer` 已删除，各玻璃表面独立渲染。组件不依赖固定浅色背景，因此可跟随深浅色模式。

6. **首次消息发送动画**  
   首条消息拥有稳定的 UUID 和时间戳。首页输入框只上报起点 frame；`ContentView` 的公共飞行协调器在顶层生成独立气泡快照，聊天页面的首个正式气泡使用无材质等尺寸占位符上报终点。快照约 0.32 秒飞抵后，正式气泡在无动画 transaction 中原子接管。

7. **后续消息发送动画**  
   `TextConversationView` 底部发送与首次发送共用同一个顶层飞行协调器。发送时立即清空输入框并插入同 UUID 的不可见占位消息；独立快照飞抵后显示正式气泡。数据库保存只替换同 UUID 的消息数据，不重建消息行。飞行完成信号由 `@State` 持有的 `@Observable` 引用对象传递，避免异步 View 值快照一直读取旧 ID、阻塞后续模型请求。

8. **气泡、键盘和模型状态**  
   气泡内容使用固有尺寸的 `VStack`，没有横向撑满用的 `Spacer`。主页面使用单行 `TextField`、`.submitLabel(.send)` 和 `.onSubmit` 直接发送。聊天页保留焦点；模型首段非空白文字到达前显示顶部三点动画。`StreamingAssistantState` 独立承载流式 token，只刷新当前回复行，完成后再并入稳定消息数组。

9. **可访问性和验证**  
   动画根据 `accessibilityReduceMotion` 自动降级；交互控件和思考状态具有辅助功能标签。当前改动使用 iPhone 17、iOS 26.5 模拟器目标完成普通构建，以及包含 13 项单元测试的测试目标编译验证。

10. **首次实时语音启动时序**  
   首次麦克风授权完成后，等待 App 回到 active 并预激活 `.playAndRecord` / `.voiceChat` 音频会话。麦克风必须在 WebSocket 冷启动完成前开始采集，连接期间的音频保存在有界本地缓冲区，收到 `session.updated` 后按顺序补发，避免用户第一次说话被丢弃。WebSocket 必须先接收 `session.created`，再发送 `session.update`；音频图无采样回调时自动重启一次。每次启动使用独立代次，结束后旧任务不得继续影响下一次会话。

完整的 Telegram iOS 对照证据、采用与拒绝项见 [`docs/telegram-ios-reference-audit.md`](docs/telegram-ios-reference-audit.md)。

## 第三部分：Liquid Glass 排障结论（2026-07-11 Side Chat）

1. **局部 modifier 相同，不代表 Preview 与真机视觉相同**  
   Liquid Glass 会根据父级合成环境、背后像素、控件 enabled/disabled 状态和系统渲染环境自适应。比较 Preview 与真机时，必须同时核对父级容器、safe area、滚动位置、背景内容和交互状态，不能只比较 `.glass` 或 `.glassEffect` 参数。

2. **本次透明度差异的根因是全屏 `GlassEffectContainer`**  
   生产页面曾用 `GlassEffectContainer(spacing: 8)` 包裹整个 `screenLayers`，而专用 Preview 没有这个父容器。虽然底部 Home、输入框和发送按钮的局部参数一致，真机仍进入统一玻璃合成环境，导致透明度和对比度明显不同。删除全屏容器后，真机表现与 Preview 一致。

3. **`GlassEffectContainer` 不是添加玻璃的必要条件**  
   `.glassEffect(...)` 和 `.buttonStyle(.glass)` 本身就能生成玻璃。容器只应负责多个玻璃形状的统一合成、邻近融合、性能优化和形状 morph。没有明确融合/变形需求时不要添加；需要时也只包裹相关的小组件组，不得为了“启用液态玻璃”包裹整屏。

4. **先清理渲染层级，再调透明度参数**  
   遇到玻璃过透、边缘不清或 Preview/真机不一致时，检查顺序应为：父级 `GlassEffectContainer` → enabled/disabled 状态 → 控件背后的实际像素 → safe area/键盘/滚动位置 → 最后才考虑 tint、实体底色或阴影。禁止在根因未明时通过叠加 tint、背景和阴影掩盖结构问题。

5. **Preview 必须覆盖真实视觉环境且不启动真实服务**  
   `TextConversationView.swift` 底部保留专用 Canvas Preview，使用真实聊天渐变、真实 `ChatMessageBubble`、可滚动长消息和固定底部栏，但不初始化数据库、蓝牙或模型请求。若生产父级渲染结构变化，必须同步更新 Preview，不能只复制局部 modifier 后声称环境一致。

## 第四部分：消息发送动画排障结论（2026-07-11）

1. **发送动画只保留一个主角**  
   首页首条消息和聊天页后续消息共用顶层飞行 overlay；输入框只上报起点，正式气泡以无材质占位符提前布局，抵达后由同 UUID 气泡原子接管。不要再叠加页面位移、缩放或两套真实玻璃视图的 `matchedGeometryEffect`。

2. **逐帧分析必须核对动画结束边界**  
   飞行层仅含正文，而正式气泡还含时间和真实 `glassEffect`，接管帧会产生轻微视觉跳变；录屏帧时间戳可用于确认是否同时发生掉帧，不能只凭正常速度播放判断。

3. **状态提示不得改变气泡终点布局**  
   首段回复前的三点思考指示器使用 `.overlay(alignment: .top)` 覆盖显示，不使用顶部 `safeAreaInset`，避免指示器出现时重排 `ScrollView` 并把已到位气泡向下推。

4. **异步流程读取持续状态而非 View 值快照**  
   飞行完成信号使用 `@State` 持有的 `@Observable` 引用对象传递；数据库保存只更新同 UUID 数据，不删除并重建消息行，避免模型请求卡住或动画结尾闪烁。

## 第五部分：预览与弹簧动画生命周期排障结论（2026-07-19）

1. **弹簧动画销毁视图必须配合 `.removed`**  
   使用带有物理长尾的弹簧动画（如 `.smooth` 或 `.spring`）时，若在动画结束回调中销毁视图（如 `overlayItem = nil`），必须显式指定 `completionCriteria: .removed`。默认的 `.logicallyComplete` 会在数值到达但物理惯性余震未消（如尺寸/位置仍有微小偏差）时提前触发回调，导致最后一帧视图突变跳跃。

## 第六部分：转场与键盘安全区排障结论（2026-07-20）

1. **转场期间忽略键盘安全区防止抖动**  
   在键盘未收起时启动转场，键盘高度变化会引起安全区（`safeArea`）高度变化，从而触发父容器（如 `GeometryReader`）及子页面垂直高度重算，导致视图跳动。在转场动画期间（`phase != .idle`），必须在父容器上动态应用 `.ignoresSafeArea(.keyboard, edges: .bottom)`，使视图垂直尺寸保持绝对稳定，待转场结束后再恢复。

2. **退场采用“定格帧”状态保持（State Preservation）**  
   退场时应避免主动重置页面组件状态（例如保持 AccessoryMenu 展开、不清理图片预览等）。将离屏页面视为后台挂起的定格快照，下次进入时无缝还原现场，保证连续、无延迟的视觉体验。
